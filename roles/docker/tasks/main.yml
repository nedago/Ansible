---
- name: Install apt packages
  apt: 
    pkg: 
    - ca-certificates
    - curl
    - gnupg
    update_cache: yes
  tags: docker

- name: Add Docker’s official GPG key
  apt_key:
    url: https://download.docker.com/linux/ubuntu/gpg
  tags: docker

- name: Add repository docker
  apt_repository:
    repo: deb [arch=arm64] https://download.docker.com/linux/ubuntu {{ ansible_lsb.codename }} stable
  tags: docker

- name: Install docker pakages
  apt: 
    pkg: 
    - docker-ce
    - docker-ce-cli
    - containerd.io
    - docker-buildx-plugin
    - docker-compose-plugin
    update_cache: yes
  tags: docker

- name: Creates directory
  file:
    path: /etc/docker
    state: directory
  tags: docker

- name: Copy file daemon.json on dest
  copy:
    src: daemon.json
    dest: /etc/docker/daemon.json
    owner: root
    group: root
    mode: 0644
  notify: Restart_service_docker
  tags: docker

- name: Add the user '{{ host_user }}' group of 'docker'
  user:
    name: "{{ user }}"
    comment: "{{ user_comment }}"
    group: docker
  tags: docker